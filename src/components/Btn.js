import { Button } from '@material-ui/core'
import React from 'react';


function Btn({color}) {
    return (
        <div>
            <Button style={{backgroundColor: color}} >BUTTONNNNNN</Button>
        </div>
    )
}

export default Btn
